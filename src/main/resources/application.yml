spring:
  application:
    name: milyoner
  datasource:
    url: jdbc:postgresql://localhost:5432/milyoner
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create # todo : will be change
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect